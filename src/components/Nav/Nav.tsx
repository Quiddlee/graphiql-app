import NavigationDrawer from '@components/Nav/ui/NavigationDrawer';

const Nav = () => {
  return (
    <nav className="h-full w-full">
      <NavigationDrawer />
    </nav>
  );
};

export default Nav;
